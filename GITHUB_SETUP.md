# إرشادات رفع المشروع على GitHub وتفعيل GitHub Pages

## الخطوات المطلوبة:

### 1. إنشاء مستودع جديد على GitHub

1. اذهب إلى [GitHub.com](https://github.com) وسجل الدخول
2. انقر على زر **"+"** في الزاوية العلوية اليمنى
3. اختر **"New repository"**
4. املأ المعلومات التالية:
   - **Repository name**: `task-management-landing-page` (أو أي اسم تفضله)
   - **Description**: `صفحة لاندنق بيج لنظام إدارة المهام`
   - اختر **Public** (لتفعيل GitHub Pages مجاناً)
   - **لا** تضع علامة على "Initialize this repository with a README"
5. انقر على **"Create repository"**

### 2. ربط المستودع المحلي بـ GitHub

بعد إنشاء المستودع على GitHub، ستظهر لك تعليمات. استخدم الأوامر التالية:

```bash
# استبدل YOUR_USERNAME و REPO_NAME بالقيم الفعلية
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git
git push -u origin main
```

**مثال:**
```bash
git remote add origin https://github.com/engbmah/task-management-landing-page.git
git push -u origin main
```

### 3. تفعيل GitHub Pages

1. اذهب إلى المستودع على GitHub
2. انقر على **Settings** (الإعدادات)
3. في القائمة الجانبية، انقر على **Pages**
4. في قسم **Source**، اختر:
   - **Branch**: `main`
   - **Folder**: `/ (root)`
5. انقر على **Save**

### 4. الوصول إلى الموقع

بعد بضع دقائق، سيكون موقعك متاحاً على:
```
https://YOUR_USERNAME.github.io/REPO_NAME/
```

**مثال:**
```
https://engbmah.github.io/task-management-landing-page/
```

---

## ملاحظات مهمة:

- ⚠️ تأكد من أن المستودع **Public** لتفعيل GitHub Pages مجاناً
- ⏱️ قد يستغرق تفعيل GitHub Pages من 1-5 دقائق
- 🔄 أي تحديثات ترفعها على GitHub سيتم تحديثها تلقائياً على الموقع
- 📧 رابط الموقع سيظهر في صفحة Settings > Pages بعد التفعيل

---

## أوامر Git الأساسية (للتحديثات المستقبلية):

```bash
# إضافة التغييرات
git add .

# عمل commit
git commit -m "وصف التغييرات"

# رفع التغييرات
git push
```

---

## استكشاف الأخطاء:

### إذا واجهت مشكلة في الرفع:
```bash
# تحقق من حالة المستودع
git status

# تحقق من الـ remote
git remote -v

# إذا كان هناك مشكلة، أعد إضافة الـ remote
git remote remove origin
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git
```

### إذا لم يظهر الموقع:
- تأكد من أن المستودع Public
- انتظر 5-10 دقائق بعد التفعيل
- تحقق من أن الملف `index.html` موجود في المجلد الرئيسي
- اذهب إلى Settings > Pages وتحقق من الحالة
